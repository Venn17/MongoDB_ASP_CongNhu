@model IEnumerable<Mongo.Models.ViewModels.AreaViewModel>

@{
    ViewData["Title"] = "Index";
}
<div class="container">
    <div class="row mt-5">
        <div class="col-12 text-center">
            <h2 style="font-family:Consolas">Management Areas</h2>
        </div>
    </div>
</div>

@if (ViewBag.Message != null)
{
    <div class="alert alert-danger" role="alert">
        <strong>Error!</strong>@ViewBag.Message
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}
<div class="container">
    <div class="row">
        <div class="col-12 d-inline-flex">
            <form class="mr-3 w-75" asp-action="Search">
                <div class="form-group form-inline">
                    <input class="form-control mr-2 w-75" name="key" style="border-radius:20px;" id="search" />
                    <button class="btn btn-default" style="border-radius:20px" type="submit">Search</button>
                </div>
            </form>
            <button class="btn btn-success w-25" style="margin-bottom:1rem;border-radius:20px">
                <a style="color:#fff" asp-controller="area" asp-action="Create">Create A New</a>
            </button>
        </div>
    </div>
</div>
<table class="table table-secondary table-hover" >
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Employees In
            </th>
            <th>
                Status
            </th>
            <th>Options</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.count)
                </td>
                <td>
                    @if (item.status)
                    {
                        <p class="text-green text-bold">Active</p>
                    }
                    else
                    {
                        <p class="text-red text-bold">No Active</p>
                    }
                </td>
                <td>
                    <a asp-controller="area" asp-action="edit" asp-route-id="@item._id" class="btn btn-warning">Edit</a>
                    <a class="btn btn-danger" asp-controller="area" asp-action="delete" asp-route-id="@item._id" onclick="return confirm('Do you want to delete this department ?')">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<nav aria-label="Page navigation example">
    <ul class="pagination">
        @if (ViewBag.totalPage > 1)
        {
            @if (ViewBag.CurrentPage > 1)
            {
                <li class="page-item mr-2"><a class="page-link" style="border-radius:20px" href="/area?page=@(ViewBag.CurrentPage - 1)">Previous</a></li>
            }
            @for (int i = 0; i < ViewBag.totalPage; i++)
            {
                if (i + 1 == ViewBag.CurrentPage)
                {
                    <li class="page-item active mr-2"><a class="page-link" style="border-radius:20px" href="/area?page=@(i + 1)">@(i + 1)</a></li>
                }
                else
                {
                    <li class="page-item mr-2"><a class="page-link" style="border-radius:20px" href="/area?page=@(i + 1)">@(i + 1)</a></li>
                }
            }
            @if (ViewBag.CurrentPage < ViewBag.totalPage)
            {
                <li class="page-item"><a class="page-link" style="border-radius:20px" href="/area?page=@(ViewBag.CurrentPage + 1)">Next</a></li>
            }
        }
    </ul>
</nav>